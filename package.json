{
  "name": "nodejs-title-project",
  "version": "1.0.0",
  "description": "A simple Node.js app displaying a title line",
  "main": "dist/app.js",
  "scripts": {
    "start": "node dist/app.js",
    "build": "cp -R views dist && npx babel src --out-dir dist --copy-files"
  },
  "dependencies": {
    "express": "^4.18.4"
  },
  "devDependencies": {
    "@babel/cli": "^7.22.5",
    "@babel/core": "^7.22.5",
    "@babel/preset-env": "^7.22.5"
  }
}

